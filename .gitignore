# Claude Code local config (contains machine-specific settings and secrets)
.claude/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment
.env
.env.*
!.env.example

# npm local cache/global (machine-specific, not project code)
.npm-cache/
.npm-global/
.npmrc
.npmrc.local

# Codex CLI artifacts
.codex-stderr.log
codex.cmd
codex.ps1

# Build archives (source of truth is the directory, not the zip)
*.zip

# Flutter build outputs
ui/build/
ui/.dart_tool/
ui/.flutter-plugins
ui/.flutter-plugins-dependencies
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies

# Flutter logs
flutter_*.log
flutter-build-*.log

# OS
.DS_Store
Thumbs.db
Thumbs.db:encryptable
